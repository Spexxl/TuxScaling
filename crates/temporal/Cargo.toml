[package]
name = "tuxscaling-temporal"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
build = "build.rs"

[dependencies]
ash.workspace = true
bytemuck.workspace = true
tuxscaling-motion = { path = "../motion" }
tuxscaling-vulkan = { path = "../vulkan" }
