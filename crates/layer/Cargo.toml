[package]
name = "tuxscaling-layer"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
tuxscaling-capture = { path = "../capture" }
ash.workspace = true
parking_lot.workspace = true
tracing.workspace = true
tuxscaling-overlay-vulkan = { path = "../overlay-vulkan" }
tuxscaling-runtime = { path = "../runtime" }
tuxscaling-vulkan = { path = "../vulkan" }
