diff --git a/sdk/toolchain.cmake b/sdk/toolchain.cmake
--- a/sdk/toolchain.cmake
+++ b/sdk/toolchain.cmake
@@ -38,7 +38,14 @@ if (MSVC)
 	set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} /W3 /GR- /fp:fast /GS /Gy")
 endif()
 
-if (FFX_API_BACKEND STREQUAL DX12_X64 OR
+if (WIN32 AND (FFX_API_BACKEND STREQUAL DX12_X64 OR
+	FFX_API_BACKEND STREQUAL VK_X64))
+
+	set(CMAKE_SYSTEM_NAME WINDOWS)
+	set(CMAKE_SYSTEM_VERSION 10.0)
+elseif (UNIX AND FFX_API_BACKEND STREQUAL VK_X64)
+	set(CMAKE_POSITION_INDEPENDENT_CODE ON)
+elseif (FFX_API_BACKEND STREQUAL DX12_X64 OR
 	FFX_API_BACKEND STREQUAL VK_X64)
 
 	set(CMAKE_RELWITHDEBINFO_POSTFIX drel)
@@ -53,10 +60,4 @@ if (FFX_API_BACKEND STREQUAL DX12_X64 OR
 	set(CMAKE_SYSTEM_NAME WINDOWS)
 	set(CMAKE_SYSTEM_VERSION 10.0)
 
-	set(CMAKE_GENERATOR_PLATFORM "x64" CACHE STRING "" FORCE)
-	set(CMAKE_VS_PLATFORM_NAME "x64" CACHE STRING "" FORCE)
-
-	# Ensure our platform toolset is x64
-	set(CMAKE_VS_PLATFORM_TOOLSET_HOST_ARCHITECTURE "x64" CACHE STRING "" FORCE)
-	
 elseif(FFX_API_BACKEND STREQUAL DX12_ARM64)
diff --git a/sdk/CMakeLists.txt b/sdk/CMakeLists.txt
--- a/sdk/CMakeLists.txt
+++ b/sdk/CMakeLists.txt
@@ -89,5 +89,9 @@ elseif(CMAKE_GENERATOR_PLATFORM STREQUAL "arm64ec" OR CMAKE_EXE_LINKER_FLAGS ST
 elseif(CMAKE_GENERATOR_PLATFORM STREQUAL "arm64ec" OR CMAKE_EXE_LINKER_FLAGS STREQUAL "/machine:ARM64EC")
 	set(FFX_PLATFORM_NAME arm64ec)
 else()
-    message(FATAL_ERROR "Unsupported target platform \"${CMAKE_GENERATOR_PLATFORM}\"")
+    if(UNIX AND CMAKE_SYSTEM_PROCESSOR MATCHES "x86_64|amd64|AMD64")
+        set(FFX_PLATFORM_NAME x64)
+    else()
+        message(FATAL_ERROR "Unsupported target platform \"${CMAKE_GENERATOR_PLATFORM}\"")
+    endif()
 endif()
@@ -159,6 +163,8 @@ if (FFX_SSSR)
 endif()
 
 # Add requred compile definitions
-add_compile_definitions(_UNICODE)
-add_compile_definitions(UNICODE)
+if(WIN32)
+    add_compile_definitions(_UNICODE)
+    add_compile_definitions(UNICODE)
+endif()
 add_compile_definitions(_DISABLE_CONSTEXPR_MUTEX_CONSTRUCTOR)
diff --git a/ffx-api/include/ffx_api/ffx_api.h b/ffx-api/include/ffx_api/ffx_api.h
--- a/ffx-api/include/ffx_api/ffx_api.h
+++ b/ffx-api/include/ffx_api/ffx_api.h
@@ -26,4 +26,8 @@ extern "C" {
 #endif  // #if defined(__cplusplus)
 
-#define FFX_API_ENTRY __declspec(dllexport)
+#if defined(_WIN32)
+#define FFX_API_ENTRY __declspec(dllexport)
+#else
+#define FFX_API_ENTRY __attribute__((visibility("default")))
+#endif
 #include <stdint.h>
diff --git a/ffx-api/CMakeLists.txt b/ffx-api/CMakeLists.txt
--- a/ffx-api/CMakeLists.txt
+++ b/ffx-api/CMakeLists.txt
@@ -74,7 +74,11 @@ file(GLOB private_ffx_api
   ${CMAKE_CURRENT_SOURCE_DIR}/src/resource/*.*
 	${CMAKE_CURRENT_SOURCE_DIR}/src/*.h
 	${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp)
 
+list(FILTER private_ffx_api EXCLUDE REGEX "ffx_provider\\.cpp$")
+list(FILTER private_ffx_api EXCLUDE REGEX "ffx_provider_fsr2\\.cpp$")
+list(FILTER private_ffx_api EXCLUDE REGEX "ffx_provider_framegeneration\\.cpp$")
+list(FILTER private_ffx_api EXCLUDE REGEX "src/resource/.*\\.rc$")
 file(GLOB public_ffx_api
 	${CMAKE_CURRENT_SOURCE_DIR}/include/ffx_api/*.h
 	${CMAKE_CURRENT_SOURCE_DIR}/include/ffx_api/*.hpp)
@@ -113,1 +113,1 @@
-add_library(amd_fidelityfx_${FFX_PLATFORM_NAME} SHARED ${private_ffx_api} ${private_ffx_api_backend_dx12} ${private_ffx_api_backend_vk} ${public_ffx_api} ${public_ffx_api_backend_dx12} ${public_ffx_api_backend_vk})
+add_library(amd_fidelityfx_${FFX_PLATFORM_NAME} STATIC ${private_ffx_api} ${public_ffx_api} ${public_ffx_api_backend_vk})
@@ -132,18 +135,18 @@ target_include_directories(amd_fidelityfx_${FFX_PLATFORM_NAME} PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/../sdk/include)
 # FSR dependency, must be built before using BuildFidelityFXSDK.bat
 
-set(FFX_FSR2 ON)
-set(FFX_FSR3 ON)
+set(FFX_FSR2 OFF)
+set(FFX_FSR3 OFF)
 set(FFX_FSR3UPSCALER ON)
-set(FFX_OF ON)
-set(FFX_FI ON)
-set(FFX_AUTO_COMPILE_SHADERS ON)
+set(FFX_OF OFF)
+set(FFX_FI OFF)
+set(FFX_AUTO_COMPILE_SHADERS OFF)
 set(FFX_BUILD_AS_DLL OFF)
 add_subdirectory(../sdk sdk)
 
-target_link_libraries(amd_fidelityfx_${FFX_PLATFORM_NAME} PRIVATE ffx_fsr2_${CMAKE_GENERATOR_PLATFORM} ffx_fsr3upscaler_${CMAKE_GENERATOR_PLATFORM} ffx_opticalflow_${CMAKE_GENERATOR_PLATFORM} ffx_frameinterpolation_${CMAKE_GENERATOR_PLATFORM} DXGI)
+target_link_libraries(amd_fidelityfx_${FFX_PLATFORM_NAME} PRIVATE ffx_fsr3upscaler_x64)
 if (FFX_API_BACKEND STREQUAL VK_X64)
 	find_package(Vulkan REQUIRED)
-	target_link_libraries(amd_fidelityfx_${FFX_PLATFORM_NAME} PRIVATE Vulkan::Headers ffx_backend_vk_${CMAKE_GENERATOR_PLATFORM})
+	target_link_libraries(amd_fidelityfx_${FFX_PLATFORM_NAME} PRIVATE Vulkan::Vulkan ffx_backend_vk_x64)
 else() # DX12
 	target_link_libraries(amd_fidelityfx_${FFX_PLATFORM_NAME} PRIVATE D3D12 ffx_backend_dx12_${CMAKE_GENERATOR_PLATFORM})
 endif()
diff --git a/sdk/src/backends/vk/CMakeLists.txt b/sdk/src/backends/vk/CMakeLists.txt
--- a/sdk/src/backends/vk/CMakeLists.txt
+++ b/sdk/src/backends/vk/CMakeLists.txt
@@ -39,1 +39,4 @@
+if(UNIX AND NOT Vulkan_LIBRARY)
+    find_file(Vulkan_LIBRARY NAMES libvulkan.so.1 PATHS /usr/lib64 /usr/lib /lib64 /lib)
+endif()
 find_package(Vulkan REQUIRED)
@@ -58,0 +59,2 @@
+list(FILTER PRIVATE_SOURCE EXCLUDE REGEX "FrameInterpolationSwapchain")
+
@@ -418,6 +420,9 @@ get_filename_component(FFX_PASS_SHADER_OUTPUT_PATH ${CMAKE_CURRENT_BINARY_DIR}/../shaders/vk ABSOLUTE)
 target_include_directories(ffx_backend_vk_${FFX_PLATFORM_NAME} PUBLIC ${FFX_INCLUDE_PATH})
 target_include_directories(ffx_backend_vk_${FFX_PLATFORM_NAME} PUBLIC ${FFX_LIB_PATH})
 target_include_directories(ffx_backend_vk_${FFX_PLATFORM_NAME} PUBLIC ${FFX_PASS_SHADER_OUTPUT_PATH})
+if(DEFINED FFX_GENERATED_SHADER_PATH)
+    target_include_directories(ffx_backend_vk_${FFX_PLATFORM_NAME} PUBLIC ${FFX_GENERATED_SHADER_PATH})
+endif()
 target_include_directories(ffx_backend_vk_${FFX_PLATFORM_NAME} PRIVATE ${FFX_COMPONENTS_PATH})
 target_include_directories(ffx_backend_vk_${FFX_PLATFORM_NAME} PRIVATE ${FFX_SHARED_PATH})
 target_include_directories(ffx_backend_vk_${FFX_PLATFORM_NAME} PRIVATE "${FFX_SRC_BACKENDS_PATH}/shared")
@@ -556,5 +556,5 @@
-add_custom_target(ffx_shader_permutations_vk DEPENDS ${FFX_SC_PERMUTATION_OUTPUTS})
-add_dependencies(${FFX_SC_DEPENDENT_TARGET} ffx_shader_permutations_vk)
-
-# Make sure shader builds are a dependency of the backend
-add_dependencies(ffx_backend_vk_${FFX_PLATFORM_NAME} ffx_shader_permutations_vk)
+if(FFX_AUTO_COMPILE_SHADERS)
+    add_custom_target(ffx_shader_permutations_vk DEPENDS ${FFX_SC_PERMUTATION_OUTPUTS})
+    add_dependencies(${FFX_SC_DEPENDENT_TARGET} ffx_shader_permutations_vk)
+    add_dependencies(ffx_backend_vk_${FFX_PLATFORM_NAME} ffx_shader_permutations_vk)
+endif()
@@ -564,1 +564,0 @@
-set_target_properties(ffx_shader_permutations_vk PROPERTIES FOLDER Backends)
diff --git a/sdk/src/backends/vk/ffx_vk.cpp b/sdk/src/backends/vk/ffx_vk.cpp
--- a/sdk/src/backends/vk/ffx_vk.cpp
+++ b/sdk/src/backends/vk/ffx_vk.cpp
@@ -341,5 +341,7 @@ FfxErrorCode ffxGetInterfaceVK(
     backendInterface->fpBreadcrumbsPrintDeviceInfo = BreadcrumbsPrintDeviceInfoVK;
     backendInterface->fpRegisterConstantBufferAllocator = RegisterConstantBufferAllocatorVK;
+#ifdef FFX_FI
     backendInterface->fpSwapChainConfigureFrameGeneration = ffxSetFrameGenerationConfigToSwapchainVK;
+#endif
 
     // Memory assignments
diff --git a/sdk/src/backends/vk/ffx_vk.cpp b/sdk/src/backends/vk/ffx_vk.cpp
--- a/sdk/src/backends/vk/ffx_vk.cpp
+++ b/sdk/src/backends/vk/ffx_vk.cpp
@@ -1120,12 +1120,10 @@
 void ConvertUTF8ToUTF16(const char* inputName, wchar_t* outputBuffer, size_t outputLen)
 {
     memset(outputBuffer, 0, outputLen * sizeof(wchar_t));
-    std::wstring_convert<std::codecvt_utf8<wchar_t>, wchar_t> converter;
-    wcscpy_s(outputBuffer, outputLen, converter.from_bytes(inputName).c_str());
+    tux_fidelityfx::utf8_to_wide(inputName, outputBuffer, outputLen);
 }
 void ConvertUTF16ToUTF8(const wchar_t* inputName, char* outputBuffer, size_t outputLen)
 {
     memset(outputBuffer, 0, outputLen * sizeof(char));
-    std::wstring_convert<std::codecvt_utf8<wchar_t>, wchar_t> converter;
-    strcpy_s(outputBuffer, outputLen, converter.to_bytes(inputName).c_str());
+    tux_fidelityfx::wide_to_utf8(inputName, outputBuffer, outputLen);
 }
diff --git a/ffx-api/src/ffx_provider.h b/ffx-api/src/ffx_provider.h
--- a/ffx-api/src/ffx_provider.h
+++ b/ffx-api/src/ffx_provider.h
@@ -26,1 +26,1 @@
-#include <FidelityFx/host/ffx_types.h>
+#include <FidelityFX/host/ffx_types.h>
diff --git a/ffx-api/src/backends.h b/ffx-api/src/backends.h
--- a/ffx-api/src/backends.h
+++ b/ffx-api/src/backends.h
@@ -26,1 +26,1 @@
-#include <FidelityFx/host/ffx_interface.h>
+#include <FidelityFX/host/ffx_interface.h>
diff --git a/sdk/include/FidelityFX/host/ffx_types.h b/sdk/include/FidelityFX/host/ffx_types.h
--- a/sdk/include/FidelityFX/host/ffx_types.h
+++ b/sdk/include/FidelityFX/host/ffx_types.h
@@ -80,1 +80,5 @@
-#define FFX_SDK_DEFAULT_CONTEXT_SIZE (1024 * 128)
+#if defined(__linux__)
+#define FFX_SDK_DEFAULT_CONTEXT_SIZE (1024 * 1024)
+#else
+#define FFX_SDK_DEFAULT_CONTEXT_SIZE (1024 * 128)
+#endif
diff --git a/sdk/include/FidelityFX/host/ffx_util.h b/sdk/include/FidelityFX/host/ffx_util.h
--- a/sdk/include/FidelityFX/host/ffx_util.h
+++ b/sdk/include/FidelityFX/host/ffx_util.h
@@ -177,3 +177,3 @@ inline uint8_t ffxCountBitsSet(uint32_t val) noexcept
-#if __cplusplus >= 202002L
+#if __cplusplus >= 202002L && !defined(__GNUC__) && !defined(__clang__)
     return static_cast<uint8_t>(std::popcount(val));

diff --git a/ffx-api/src/ffx_provider_fsr3upscale.cpp b/ffx-api/src/ffx_provider_fsr3upscale.cpp
--- a/ffx-api/src/ffx_provider_fsr3upscale.cpp
+++ b/ffx-api/src/ffx_provider_fsr3upscale.cpp
@@ -71,1 +71,1 @@ uint64_t ffxProvider_FSR3Upscale::GetId() const
-    return 0xF5A5'CA1Eui64 << 32 | (FFX_SDK_MAKE_VERSION(FFX_FSR3UPSCALER_VERSION_MAJOR, FFX_FSR3UPSCALER_VERSION_MINOR, FFX_FSR3UPSCALER_VERSION_PATCH) & 0xFFFF'FFFF);
+    return 0xF5A5'CA1EULL << 32 | (FFX_SDK_MAKE_VERSION(FFX_FSR3UPSCALER_VERSION_MAJOR, FFX_FSR3UPSCALER_VERSION_MINOR, FFX_FSR3UPSCALER_VERSION_PATCH) & 0xFFFF'FFFF);
diff --git a/ffx-api/CMakeLists.txt b/ffx-api/CMakeLists.txt
--- a/ffx-api/CMakeLists.txt
+++ b/ffx-api/CMakeLists.txt
@@ -52,8 +52,8 @@ set(CMAKE_DEBUG_POSTFIX d)
-set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_HOME_DIRECTORY}/bin)
-set(CMAKE_RUNTIME_OUTPUT_DIRECTORY_DEBUG ${CMAKE_HOME_DIRECTORY}/bin)
-set(CMAKE_RUNTIME_OUTPUT_DIRECTORY_RELEASE ${CMAKE_HOME_DIRECTORY}/bin)
-set(CMAKE_RUNTIME_OUTPUT_DIRECTORY_RELWITHDEBINFO ${CMAKE_HOME_DIRECTORY}/bin)
-set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY ${CMAKE_HOME_DIRECTORY}/bin)
-set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_DEBUG ${CMAKE_HOME_DIRECTORY}/bin)
-set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_RELEASE ${CMAKE_HOME_DIRECTORY}/bin)
-set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_RELWITHDEBINFO ${CMAKE_HOME_DIRECTORY}/bin)
+set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/bin)
+set(CMAKE_RUNTIME_OUTPUT_DIRECTORY_DEBUG ${CMAKE_CURRENT_BINARY_DIR}/bin)
+set(CMAKE_RUNTIME_OUTPUT_DIRECTORY_RELEASE ${CMAKE_CURRENT_BINARY_DIR}/bin)
+set(CMAKE_RUNTIME_OUTPUT_DIRECTORY_RELWITHDEBINFO ${CMAKE_CURRENT_BINARY_DIR}/bin)
+set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/bin)
+set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_DEBUG ${CMAKE_CURRENT_BINARY_DIR}/bin)
+set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_RELEASE ${CMAKE_CURRENT_BINARY_DIR}/bin)
+set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_RELWITHDEBINFO ${CMAKE_CURRENT_BINARY_DIR}/bin)
diff --git a/sdk/CMakeLists.txt b/sdk/CMakeLists.txt
--- a/sdk/CMakeLists.txt
+++ b/sdk/CMakeLists.txt
@@ -121,3 +121,3 @@ if (NOT FFX_CUSTOM_API)
-	set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_DEBUG   ${CMAKE_HOME_DIRECTORY}/bin/ffx_sdk/)
-	set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_RELEASE ${CMAKE_HOME_DIRECTORY}/bin/ffx_sdk/)
-	set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_RELWITHDEBINFO ${CMAKE_HOME_DIRECTORY}/bin/ffx_sdk/)
+	set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_DEBUG   ${CMAKE_CURRENT_BINARY_DIR}/bin/ffx_sdk/)
+	set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_RELEASE ${CMAKE_CURRENT_BINARY_DIR}/bin/ffx_sdk/)
+	set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_RELWITHDEBINFO ${CMAKE_CURRENT_BINARY_DIR}/bin/ffx_sdk/)
